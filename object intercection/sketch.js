// Project Title
// Your Name
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"
let nodes = [];
let reach = 150;

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
  for(let n )
}

function mousepressed(){
  if(mouseButton==="Left"){
    let x = random(-50,50);
    let y = random(-50,50);
  }
}

class csNode{
  constructor(x,y){
    this.x = x; this.y = y; this.size = 5;
    this.c = color(random(255), random(255), random(255));
    this.xTime = random(10);  this.yTime = random(100);
    this.timeShift = 0.01; this.maxSpeed = 5;

    display(){
      fill
    }
  }
}